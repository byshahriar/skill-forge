---
description: Implement the plan one verified slice at a time
---

Invoke the implement skill (with tdd for every logic change).

Work through the current plan one task at a time: write the failing test, make it pass, verify the slice against its acceptance criteria, then move on. Pause on failures or scope surprises rather than pushing through.
