description = "Get unstuck — dispatch the right lateral-thinking technique"

prompt = """
Invoke the problem-solving skill.

Identify the stuck-type first (complexity spiraling / forced assumptions / need breakthrough / recurring shapes / scale uncertainty), apply the matching technique deliberately, and record what was tried. If code is misbehaving, route to debug instead — that's evidence work, not creativity work.
"""
