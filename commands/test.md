---
description: Prove behavior with tests — red, green, refactor
---

Invoke the tdd skill.

For the current change or bug: write a failing test that captures the expected behavior first, implement until it passes, and run the full suite. For bug reports, reproduce the bug as a test before fixing it.
